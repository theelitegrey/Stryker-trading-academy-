// Stryker Trading Academy — bundled Trading Models seed data
//
// Mirrors assets/chapters-data.js: this is the fallback/bundled source of
// truth that ships with the site. Firestore's `models` collection is the
// live, editable source once seeded — this file is what "Reset from bundled
// content" and "Update all from bundled content" write from, and what the
// site falls back to if Firestore is empty or unreachable.
//
// Shape of a model:
// {
//   id: "silver-bullet",          // slug, used as the Firestore doc ID and URL param — lowercase, hyphenated, no spaces
//   name: "ICT Silver Bullet",
//   category: "Time-based",       // free text — shown as a tag on the model card
//   summary: "One or two sentence description shown on the models list card.",
//   video: "",                    // optional video URL, same as chapters
//   bodyHtml: "<p>...</p>",       // full write-up, same rich-text format as chapter bodyHtml
//   paragraphs: [],               // derived plain-text fallback, auto-generated by the editor
//   steps: [                      // the model's rules/criteria, same shape as chapter lessons
//     { title: "Step 1 title", desc: "plain text", descHtml: "<p>...</p>" }
//   ]
// }
//
// Currently empty — populate through the Trading Models admin editor, or ask
// Claude to add specific models once you send the content.
const MODELS_SEED = [];
