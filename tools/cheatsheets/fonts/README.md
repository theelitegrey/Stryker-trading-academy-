Static weight instances of Space Grotesk (500/600/700) and JetBrains Mono (700/800),
cut from the variable fonts with fontTools.varLib.instancer. Both fonts are SIL Open
Font License 1.1. Static files are used because Chrome embeds variable fonts in a
printed PDF as Type3 glyphs; static TTFs embed as proper TrueType subsets.
